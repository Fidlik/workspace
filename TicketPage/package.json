{
  "name": "ticket-page",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "dev": "wrangler pages dev . --d1 DB=local-ticket-db",
    "deploy": "wrangler pages deploy ."
  }
}